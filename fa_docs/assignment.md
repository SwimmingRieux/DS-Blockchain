# پروژه: ساخت یک نود بلاک‌چین UTXO

این پروژه شما را در فرآیند پیاده‌سازی یک بلاک‌چین حداقلی مبتنی بر UTXO، همراه با یک رابط خط فرمان (CLI) برای تعامل با نود و ماینر خود، راهنمایی می‌کند. این پروژه برای کسانی که به تازگی وارد دنیای توسعه بلاک‌چین شده‌اند طراحی شده و به چندین هدف قابل مدیریت تقسیم شده است.

## 🎯 اهداف یادگیری

پس از اتمام موفقیت‌آمیز این پروژه، شما قادر خواهید بود:

- **درک و پیاده‌سازی حسابداری UTXO:** اصول مدل خروجی تراکنش‌های خرج‌نشده (UTXO) و نحوه استفاده از امضاهای دیجیتال برای صدور مجوز تراکنش‌ها را درک کنید.
- **ساخت یک ممپول با اولویت‌بندی بر اساس کارمزد:** یک ممپول (memory pool) را به عنوان یک گراف جهت‌دار غیرمدور (DAG) برای ذخیره تراکنش‌های تأییدنشده پیاده‌سازی کنید و آن‌ها را بر اساس کارمزد به ازای هر بایت اولویت‌بندی کنید.
- **ساخت و استخراج بلاک‌ها:** یک قالب بلاک را با انتخاب حریصانه تراکنش‌ها از ممپول توسعه دهید، به طوری که اندازه بلاک از یک حد ثابت تجاوز نکند. سپس بلاک را با استفاده از الگوریتم اثبات کار (PoW) استخراج خواهید کرد.
- **تولید و تأیید اثبات‌های مرکل:** اثبات‌های مرکل را برای اثبات کارآمد وجود یک تراکنش در یک بلاک ایجاد و اعتبارسنجی کنید.

## 🚀 شروع کار

۱. **با مفاهیم آشنا شوید:** قبل از شروع کدنویسی، اطمینان حاصل کنید که درک کاملی از مبانی اصلی بلاک‌چین دارید. [اسناد مفهومی](index.md#learning-materials) را برای یادگیری در مورد بلاک‌ها، تراکنش‌ها، درختان مرکل و موارد دیگر مرور کنید.
۲. **اهداف را دنبال کنید:** پروژه به چندین هدف تقسیم شده است. [اهداف و چک‌لیست‌ها](milestones.md) را برای راهنمایی در فرآیند پیاده‌سازی خود دنبال کنید.
۳. **با ساختارهای داده شروع کنید:** با پیاده‌سازی ساختارهای داده اصلی مانند `Block`، `Transaction` و `Mempool` شروع کنید.

## 📋 وظایف پروژه

وظیفه اصلی شما ساخت یک نود CLI است که بتواند اقدامات زیر را انجام دهد:

- **`GiveKeyPair`:** یک جفت کلید عمومی/خصوصی جدید تولید کنید.
- **`SetDifficulty`:** سختی استخراج بلاک‌چین را تنظیم کنید.
- **`AddTransactionToMempool`:** یک تراکنش جدید به ممپول اضافه کنید.
- **`EvictMempool`:** تراکنش‌ها را از ممپول خارج کنید.
- **`MineBlock`:** یک بلاک جدید استخراج کنید.
- **`GetMerkleProof`:** یک اثبات مرکل برای یک تراکنش مشخص دریافت کنید.
- **`VerifyTxInclusion`:** تأیید کنید که یک تراکنش در یک بلاک وجود دارد.

برای پشتیبانی از این عملکرد، شما باید ساختارهای داده زیر را پیاده‌سازی کنید:

- **بلاک‌چین:** یک لیست پیوندی از بلاک‌ها.
- **نقشه UTXO:** یک ساختار داده برای پیگیری خروجی‌های تراکنش‌های خرج‌نشده.
- **ممپول DAG:** یک گراف جهت‌دار غیرمدور برای ذخیره و مدیریت تراکنش‌های تأییدنشده.
- **درختان متوازن:** دو درخت متوازن برای امتیازات اجداد و نوادگان در ممپول.

## ⛓️ محدودیت‌ها و قوانین

برای اطمینان از سازگاری و ارزیابی منصفانه، پیاده‌سازی شما باید به محدودیت‌های زیر پایبند باشد:

- **محدودیت اندازه بلاک:** ۲۰۰۰ بایت (شامل هدر و تمام تراکنش‌ها).
- **تابع هش:** SHA-256 باید به طور مداوم برای تمام عملیات‌های هشینگ استفاده شود.

پیاده‌سازی شما همچنین باید قوانین اعتبارسنجی زیر را پشت سر بگذارد:

- ورودی‌ها باید به UTXOهای موجود ارجاع دهند.
- امضاهای دیجیتال باید معتبر باشند.
- هیچ‌گونه خرج کردن مجدد UTXOها مجاز نیست.
- کارمزد تراکنش‌ها باید غیرمنفی (≥ ۰) باشد.
- اندازه کل بلاک نباید از ۲۰۰۰ بایت تجاوز کند.
- هش بلاک باید سختی PoW را برآورده کند.
- ریشه مرکل در هدر بلاک باید صحیح باشد.

---
[بازگشت به فهرست](index.md) · [بعدی: اهداف و چک‌لیست‌ها →](milestones.md)